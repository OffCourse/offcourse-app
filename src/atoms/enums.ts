export enum Variant {
  DEFAULT = "DEFAULT",
  DISABLED = "DISABLED",
  INFO = "INFO",
  POSITIVE = "POSITVE",
  WARNING = "WARNING",
  NEGATIVE = "NEGATIVE"
}

const { DEFAULT } = Variant;

export enum Size {
  SMALL = "SMALL",
  NORMAL = "NORMAL",
  LARGE = "LARGE"
}
